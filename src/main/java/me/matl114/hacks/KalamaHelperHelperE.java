package me.matl114.hacks;

import java.util.ArrayList;
import java.util.List;
import me.matl114.utils.commands.params.api.CommandExecution;
import me.matl114.utils.commands.params.api.InputArgument;

public class KalamaHelperHelperE extends KalamaHelperHelperZX {
    public List<String> tab = new ArrayList<>();

    public KalamaHelperHelperE(CommandExecution player, List<InputArgument<?>> arguments) {
        super(KalamaHelperHelperZ.UK, player, arguments);
    }
}
